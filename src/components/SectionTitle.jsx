const SectionTitle = ({ title }) => {
  return (
    <h3 className="text-center mt-6 mb-10 text-2xl md:text-3xl font-serif tracking-wide text-emas-green">
      {title}
    </h3>
  );
};

export default SectionTitle;
